-----------------------------------------------------描述: GS通用网络消息处理--时间: 2012.7.12--作者: liujh---------------------------------------------------MsgConnectReply = {}local p = MsgConnectReply;function p.ShowLoginRegUI(netdata)    LogInfo("MsgConnectReply.ShowLoginRegUI2");        local nAccount = netdata:ReadInt();    local nErrorCode = netdata:ReadInt();    LogInfo("nErrorCode:[%d]",nErrorCode);    if(nErrorCode == 2) then        QuitGame();        --Login_ServerUI.LoadUI();        CommonDlgNew.ShowYesDlg("您的帐号已在其它地方登录！");    else        Login_RegRoleUI.LoadUI();    end    	return true;endRegisterNetMsgHandler(NMSG_Type._MSG_CONNECTREPLY,"p.ShowLoginRegUI",p.ShowLoginRegUI);